name: finpilot_mobile
description: "FinPilot - Agentic Mobile Financial Advisor"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  fl_chart: ^0.69.0
  flutter_markdown: ^0.7.4
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
